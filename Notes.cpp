#include <iostream>
#include<ctime>
#include<cstdlib>

class line {
private:
    std::string str;
public:
    void SetStr(int s) {
        str = s;
    }
    int GetTime(){
        //monitor clock, retern SS, HH, DD;
    }
};

class block : public line{
private:
    //array of str
public:
    //get all notes per day
    //get all blocks per month
    //del last block per month
};

int main()
{
    
}

